name: Build docker image for devopsjs-test

on:
  push:
    branches: ["master"]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: checkout code
      uses: actions/checkout@v3

    - name: build docker image
      run: docker build -t devops-js-test .
